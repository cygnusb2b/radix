<div class="col-md-4">
    {{#-browse/list routeName='core.users' items=model as |item|}}
        <h5 class="list-group-item-heading">{{item.model.givenName}} {{item.model.familyName}}</h5>
        <p class="list-group-item-text">{{item.model.email}}</p>
    {{/-browse/list}}
</div>
<div class="col-md-6">{{ outlet }}</div>
