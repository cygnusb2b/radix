{{#nav-breadcrumbs as |crumbs|}}
  {{#crumbs.item active=true}}{{entypo-icon "news"}} Form Definitions{{/crumbs.item}}
{{/nav-breadcrumbs}}

<div class="row">
    <div class="col">
        {{#-browse/list routeName='form.definitions' items=model as |item|}}
            <h5 class="list-group-item-heading">{{item.model.name}}</h5>
            <p class="list-group-item-text">{{item.model.label}}</p>
        {{/-browse/list}}
    </div>
</div>
