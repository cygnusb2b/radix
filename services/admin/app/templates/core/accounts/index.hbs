{{#nav-breadcrumbs as |crumbs|}}
  {{#crumbs.item active=true}}{{entypo-icon "network"}} Core Accounts{{/crumbs.item}}
{{/nav-breadcrumbs}}

<div class="row">
    <div class="col">
        {{#-browse/list routeName='core.accounts' canCreate=true items=model as |item|}}
            <h5 class="list-group-item-heading">{{item.model.name}}</h5>
            <p class="list-group-item-text">{{item.model.key}}</p>
        {{/-browse/list}}
    </div>
</div>
