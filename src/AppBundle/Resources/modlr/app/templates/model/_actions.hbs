{{#if model.hasDirtyAttributes}}
    {{#if model.isSaving}}
        <button class="spin"><i class="ion-radio-waves"></i></button>
    {{else}}
        <button class="unsaved" {{action 'saveModel' model}}><i class="ion-android-done"></i></button>
        {{#if model.isNew}}
            <button class="delete" {{action 'undoChanges' model 'model'}}><i class="ion-android-close"></i></button>
        {{else}}
            <button class="undo" {{action 'undoChanges' model 'model.edit'}}><i class="ion-reply"></i></button>
        {{/if}}
    {{/if}}
{{else}}
    <button class="saved" {{action 'saveModel' model}}><i class="ion-android-done"></i></button>
{{/if}}
