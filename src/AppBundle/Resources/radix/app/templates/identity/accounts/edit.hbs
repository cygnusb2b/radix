<div class="card">
    <div class="card-header">
        <ul class="nav nav-tabs card-header-tabs pull-xs-left">
            {{card-tab-item icon='ion-document' text='General' active=true target='general'}}
            {{card-tab-item icon='ion-information-circled' text='Info' target='info'}}
        </ul>
        {{form-close class='pull-right' transitionTo='identity.accounts'}}
    </div>
    <form>
        <div class="card-block">
            {{card-tab-block key='general' model=model  template='identity/accounts/-general'}}
            {{card-tab-block key='info'    model=model  template='identity/accounts/-info'}}
        </div>
        <div class="card-footer">
            {{model-actions/save model=model transitionTo='identity.accounts.edit'}}

            {{#if model.isNew}}
                {{model-actions/undo model=model confirmValue=model.name transitionTo='identity.accounts'}}
            {{else}}
                {{model-actions/undo model=model confirmValue=model.name transitionTo='identity.accounts.edit'}}
            {{/if}}
        </div>
    </form>
</div>
