<div class="card">
    <div class="card-header">
        <ul class="nav nav-tabs card-header-tabs pull-xs-left">
            {{card-tab-item icon='ion-document' text='General' active=true target='general'}}
            {{card-tab-item icon='ion-information-circled' text='Info' target='info'}}
        </ul>
        {{form-close class='pull-right' transitionTo='customer.accounts'}}
    </div>
    <form>
        <div class="card-block">
            {{card-tab-block key='general' model=model  template='customer/accounts/-general'}}
            {{card-tab-block key='info'    model=model  template='customer/accounts/-info'}}
        </div>
        <div class="card-footer">
            {{model-actions/save model=model transitionTo='customer.accounts.edit'}}

            {{#if model.isNew}}
                {{model-actions/undo model=model confirmValue=model.name transitionTo='customer.accounts'}}
            {{else}}
                {{model-actions/undo model=model confirmValue=model.name transitionTo='customer.accounts.edit'}}
            {{/if}}
        </div>
    </form>
</div>
