<div class="col-md-4">
    {{#-browse/list routeName='demographic.choices' canCreate=false items=model as |item|}}
        <h5 class="list-group-item-heading">{{item.model.name}}</h5>
        <p class="list-group-item-text">{{item.model.question.name}}</p>
    {{/-browse/list}}
</div>
<div class="col-md-6">{{ outlet }}</div>
