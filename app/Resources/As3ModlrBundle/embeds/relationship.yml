relationship:
    uses: [ 'fieldable' ]
    attributes:
        relType:
            type: string
            description: The relationship type. Either one or many.
        relatedModel:
            type: string
            description: The model key this relationship references.
        invserseField:
            type: string
            description: The inverse field name. A non-null value signifies that this relationship is inverse.
