customer-email-verification:
    attributes:
        verified:
            type: boolean
            defaultValue: false
        token:
            type: string # The verification token. Used when verifying an email.
        completedDate:
            type: date # The date the verification was completed
        generatedDate:
            type: date # The date the verification token was generated
