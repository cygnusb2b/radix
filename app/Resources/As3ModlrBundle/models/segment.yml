segment:
    entity:
        persistence:
            key: mongodb
            db: radix
    mixins: [ 'keyable' ]
    relationships:
        workspace:
            entity: workspace
            type: one
            description: The workspace that owns the segment.
