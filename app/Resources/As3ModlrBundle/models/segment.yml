segment:
    entity:
        persistence:
            key: mongodb
            db: modlr-space
    mixins: [ 'keyable' ]
    relationships:
        workspace:
            entity: workspace
            type: one
            description: The workspace that owns the segment.
