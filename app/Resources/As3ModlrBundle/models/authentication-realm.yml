authentication-realm:
    entity:
        polymorphic: true
        persistence:
            key: mongodb
            db: radix
    mixins: [ 'timestampable', 'keyable' ]
    attributes:
        roles:
            type: array
            defaultValue: []
        defaultRoles:
            type: array
            defaultValue: []
