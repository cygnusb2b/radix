input-answer:
    entity:
        abstract: true
        polymorphic: true
        persistence:
            key: mongodb
            db: radix
    mixins: [ 'timestampable', 'question-answerable' ]
    relationships:
        submission:
            type: one
            entity: input-submission
