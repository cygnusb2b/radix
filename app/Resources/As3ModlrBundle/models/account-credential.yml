account-credential:
    entity:
        polymorphic: true
        abstract: true
        persistence:
            key: mongodb
            db: radix
    relationships:
        account:
            type: one
            entity: identity-account
            description: 'The identity account that owns this credential.'

