demographic-label:
    entity:
        persistence:
            key: mongodb
            db: radix
    mixins: [ 'timestampable' ]
    attributes:
        name:
            type: string
            description: The name of the label.
