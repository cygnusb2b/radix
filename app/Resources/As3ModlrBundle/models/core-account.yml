core-account:
    entity:
        persistence:
            key: mongodb
            db: radix
    mixins: [ 'keyable', 'timestampable' ]
    relationships:
        applications:
            type: many
            inverse: true
            field: account
            entity: core-application
